package com.trainguard.backend.userActions;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserContactUsRepository extends JpaRepository<UserContactUsEntity, Long> {
}
